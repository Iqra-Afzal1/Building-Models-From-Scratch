# Mobile Price Classification using K-Nearest Neighbors (KNN) From Scratch
# Median House Price Regressiom using K-Nearest Neighbors (KNN) From Scratch

A complete implementation of the **K-Nearest Neighbors (KNN)** classification algorithm **from scratch using NumPy**.

The project demonstrates how KNN works internally, compares different distance metrics, performs hyperparameter tuning using **5-Fold Cross Validation**, and visualizes the classification results using PCA.

---

## Project Overview

The notebook covers:

- Data preprocessing
- PCA visualization
- Feature scaling
- KNN implementation from scratch
- Euclidean and Manhattan distance metrics
- Hyperparameter tuning (K selection)
- Cross Validation
- Model evaluation
- Prediction visualization
- Sklearn Comparison

---

## Project Structure

```
KNN
│
├── dataset/
│   └── train.csv
├── notebooks/
│   └── knn_classification.ipynb
│   └── knn_regression.ipynb
|
├── visualizations/
│   └── classification
│   |    └── class_distribution_2d_pca.png
│   |    └── predictions_2d_pca.png
│   |    └── optimum_k_scratch_implemented.png
│   |    └── optimum_k_sklearn_model.png
|   |
│   └── regression
│       └── distribution_2d_pca.png
│       └── predictions_2d_pca.png
│       └── optimum_k_scratch_implemented.png
│       └── optimum_k_sklearn_model.png

│
└── README.md

```

